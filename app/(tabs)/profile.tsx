import { ProfileSearch } from '#/screens/Profile/ProfileSearch';

export default function Profile() {
  // Use the existing ProfileSearch component as a placeholder
  // This will show a profile search interface
  return <ProfileSearch route={{ name: 'ProfileSearch', params: {} } as any} navigation={{} as any} />;
}
